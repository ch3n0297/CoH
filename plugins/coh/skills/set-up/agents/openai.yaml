interface:
  display_name: "CoH Set Up"
  short_description: "Assess and plan a repository harness safely"
  default_prompt: "Use $coh:set-up to assess this repository and produce a read-only canonical CoH BuildPlan v1 with its digest."
policy:
  allow_implicit_invocation: false
